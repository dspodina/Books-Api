<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/css/style.css" />
        <title>Update Book</title>
    </head>
    <body>
        <header>
            <h1>Update Book</h1>
            <%- include("./includes/nav.ejs")%>
        </header>

        <form action="/api/update/<%= book.id %>" method="POST">
            <label for="title">Title</label>
            <input
                type="text"
                id="title"
                name="title"
                value="<%=book.title %>"
                required
            />
            <br />
            <label for="genre">Genre</label>
            <input
                type="text"
                id="genre"
                name="genre"
                value="<%=book.genre%>"
                required
            />
            <br />
            <label for="Year">Year</label>
            <input
                type="text"
                id="year"
                name="year"
                value="<%=book.year %>"
                required
            />
            <br />
            <label for="img">Image URL</label>
            <input
                type="url"
                id="img"
                name="img"
                value="<%=book.img %>"
                required
            />
            <br />
            <button type="submit">Submit</button>
        </form>
    </body>
</html>